% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-censo-superior.R
\name{get_censo_superior}
\alias{get_censo_superior}
\title{Get Higher Education Census (Censo da Educação Superior) data}
\usage{
get_censo_superior(
  year,
  type = c("ies", "cursos", "alunos", "docentes"),
  uf = NULL,
  n_max = Inf,
  keep_zip = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{year}{The year of the census (2009-2024).}

\item{type}{Type of data to load. Options:
\itemize{
\item \code{"ies"}: Higher education institutions (default)
\item \code{"cursos"}: Undergraduate courses
\item \code{"alunos"}: Student enrollment
\item \code{"docentes"}: Faculty/professors
}}

\item{uf}{Optional. Filter by state (UF code or abbreviation).}

\item{n_max}{Maximum number of rows to read. Default is \code{Inf} (all rows).
Consider using a smaller value for exploration.}

\item{keep_zip}{Logical. If \code{TRUE}, keeps the downloaded ZIP file in cache.}

\item{quiet}{Logical. If \code{TRUE}, suppresses progress messages.}
}
\value{
A tibble with Higher Education Census microdata in tidy format.
}
\description{
Downloads and processes microdata from the Brazilian Higher Education
Census (Censo da Educação Superior), conducted annually by INEP.
Returns data on institutions, courses, students, or faculty.
}
\details{
The Higher Education Census is the most comprehensive statistical survey
on higher education institutions (HEIs) in Brazil. It collects data from
all HEIs offering undergraduate and graduate programs.

\strong{Data types:}
\itemize{
\item \code{"ies"}: One row per institution — administrative data, location,
academic organization, funding type.
\item \code{"cursos"}: One row per undergraduate course — area of study, modality
(in-person/distance), enrollment counts.
\item \code{"alunos"}: One row per student enrollment — demographics, program,
admission type, enrollment status.
\item \code{"docentes"}: One row per faculty member — education level, employment
type, teaching regime.
}

\strong{Important notes:}
\itemize{
\item Student files (\code{"alunos"}) can be very large (several GB).
Use \code{n_max} to read a sample first.
\item Column names are standardized to lowercase with underscores.
\item Use the \code{uf} parameter to filter by state for faster processing.
}
}
\section{Data dictionary}{

For detailed information about variables, see INEP's documentation:
\url{https://www.gov.br/inep/pt-br/acesso-a-informacao/dados-abertos/microdados/censo-da-educacao-superior}
}

\examples{
\dontrun{
# get institution data for 2023
ies <- get_censo_superior(2023)

# get course data for Sao Paulo
cursos_sp <- get_censo_superior(2023, type = "cursos", uf = "SP")

# get a sample of student data
alunos <- get_censo_superior(2023, type = "alunos", n_max = 10000)

# get faculty data
docentes <- get_censo_superior(2023, type = "docentes")
}
}
\seealso{
Other Higher Education Census functions: 
\code{\link{list_censo_superior_files}()}
}
\concept{Higher Education Census functions}
